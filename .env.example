# Copy this file to .env on the droplet and fill in the values.
# .env is excluded from git — never commit real secrets.

SECRET_KEY=change-me-to-a-long-random-string
CAPELLA_TOOLS_PATH=/opt/capella_tools
