# capella_model_7_0_1

Placeholder folder for a Capella 7.0.1 model, published here via eGit from Capella Studio.

This file exists only so the folder is tracked by git before the real model content is pushed — delete or replace it once the `.aird`/`.capella`/`.afm` files are in place.
